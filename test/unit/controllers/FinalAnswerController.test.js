"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/FinalAnswerController');

describe('controllers:FinalAnswerController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
