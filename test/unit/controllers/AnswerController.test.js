"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/AnswerController');

describe('controllers:AnswerController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
