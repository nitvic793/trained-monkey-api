"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/FinalAnswer');
const Fixture = require('../../fixtures/FinalAnswerFixture');

describe('models:FinalAnswer', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
