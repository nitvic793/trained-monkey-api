"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Question');
const Fixture = require('../../fixtures/QuestionFixture');

describe('models:Question', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
