"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/QuestionController');

describe('controllers:QuestionController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
