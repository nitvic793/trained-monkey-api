"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Answer');
const Fixture = require('../../fixtures/AnswerFixture');

describe('models:Answer', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
